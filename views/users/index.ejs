<%- include('../cards', {
    cards: users.map(user => ({
        title: user.title,
        text: user.description, 
        image: user.image,
        url: '/korystuvachi/' + user.slug
    }))
}); %>