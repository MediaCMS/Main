<%- include('../cards', {
    cards: posts.map(post => ({
        title:  post.title,
        text: post.description, 
        image: post.image,
        url: '/posts/' + post.slug,
        button: {
            text: post.category.title,
            url: '/categories/' + post.category.slug
        }
    }))
}); %>