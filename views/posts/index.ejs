<%- include('../cards', {
    cards: posts.map(post => ({
        title: post.title,
        subTitle: {
            text: post.user.title,
            url: '/users/' + post.user.slug
        },
        text: post.description, 
        image: post.image,
        url: '/posts/' + post.slug,
        button: {
            text: post.category.title,
            url: '/categories/' + post.category.slug
        }
    }))
}); %>