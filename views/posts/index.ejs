<%- include('../cards', {
    cards: posts.map(post => ({
        title: post.title,
        subTitle: {
            text: post.user.title,
            url: '/автори/' + post.user.slug
        },
        text: post.description, 
        image: post.image,
        url: '/публікації/' + post.slug,
        button: {
            text: post.category.title,
            url: '/категорії/' + post.category.slug
        }
    }))
}); %>