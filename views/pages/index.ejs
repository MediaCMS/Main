<%- include('../cards', {
    cards: pages.map(page => ({
        title: page.title,
        text: page.description,
        image: page.image.url,
        url: '/storinky/' + page.slug,
    }))
}); %>